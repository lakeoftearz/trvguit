<p>
	A list of all flight companies
	</p>
	<p>
	<%= link_to "Create new flight company",  newfc_path %>
	</p> 

  <% @fcomps.in_groups_of(5, false) do |f| %>

  
  <% f.each do |fcomp| %>
 
  <div class="col-md-1 col-xs-6">
           <div class="thumbnail" > 
     <%= image_tag fcomp.logo.url, :size => "50x50" unless fcomp.logo.blank? %> 
     <div class="caption"> <%= fcomp.name  %>  <%= link_to "- Edit", edit_fcomp_path(fcomp.id) %>
     	</div>
     </div>
     </div>
  
   	<% end %>
   	
   	
    <% end %>
    </div>